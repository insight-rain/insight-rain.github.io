# SpikySpace: A Spiking State Space Model for Energy-Efficient Time Series Forecasting

**相关性评分**: 6.0/10

**排名**: #9


---


## 基本信息

- **arXiv ID**: [2601.02411v1](https://arxiv.org/abs/2601.02411v1)
- **发布时间**: 2026-01-02T13:10:53Z
- **相关性评分**: 6.0/10
- **是否相关**: 是

## 作者

Kaiwen Tang, Jiaqi Zheng, Yuze Jin, Yupeng Qiu, Guangda Sun, Zhanglu Yan, Weng-Fai Wong

## 关键词

Inference Efficiency, Lightweight Architecture, Edge Deployment

## 一句话总结

SpikySpace是一种基于脉冲状态空间模型的能量高效时间序列预测方法，专注于在边缘设备上实现低能耗和实时响应。

## 摘要

Time-series forecasting often operates under tight power and latency budgets in fields like traffic management, industrial condition monitoring, and on-device sensing. These applications frequently require near real-time responses and low energy consumption on edge devices. Spiking neural networks (SNNs) offer event-driven computation and ultra-low power by exploiting temporal sparsity and multiplication-free computation. Yet existing SNN-based time-series forecasters often inherit complex transformer blocks, thereby losing much of the efficiency benefit. To solve the problem, we propose SpikySpace, a spiking state-space model (SSM) that reduces the quadratic cost in the attention block to linear time via selective scanning. Further, we replace dense SSM updates with sparse spike trains and execute selective scans only on spike events, thereby avoiding dense multiplications while preserving the SSM's structured memory. Because complex operations such as exponentials and divisions are costly on neuromorphic chips, we introduce simplified approximations of SiLU and Softplus to enable a neuromorphic-friendly model architecture. In matched settings, SpikySpace reduces estimated energy consumption by 98.73% and 96.24% compared to two state-of-the-art transformer based approaches, namely iTransformer and iSpikformer, respectively. In standard time series forecasting datasets, SpikySpace delivers competitive accuracy while substantially reducing energy cost and memory traffic. As the first full spiking state-space model, SpikySpace bridges neuromorphic efficiency with modern sequence modeling, marking a practical and scalable path toward efficient time series forecasting systems.

## 详细分析

## SpikySpace 论文详细摘要

**1. 研究背景和动机**
在交通管理、工业监测等边缘计算场景中，时间序列预测任务面临着严格的功耗和延迟约束。脉冲神经网络因其事件驱动、无乘法运算的特性，是实现超低功耗边缘智能的潜在方案。然而，现有的基于SNN的时间序列预测模型往往继承了Transformer等复杂架构，导致其能效优势大打折扣。因此，亟需一种既能保持高预测精度，又能充分发挥SNN能效优势的新型模型。

**2. 核心方法和技术创新**
本文提出了 **SpikySpace**，这是首个**完全脉冲化的状态空间模型**。其核心创新在于：
- **脉冲驱动的状态空间建模**：将状态空间模型（SSM）的线性时间复杂度和长程依赖建模能力，与SNN的稀疏事件驱动计算相结合。通过**选择性扫描**机制，仅在脉冲事件发生时更新状态，将计算复杂度从Transformer的O(L²)降至O(L)，并避免了密集乘法。
- **硬件友好的激活函数近似**：针对SSM中关键的SiLU和Softplus激活函数，提出了**PTSoftplus**和**PTSiLU**两种近似方法。它们使用基于2的幂次和线性变换，替代了昂贵的指数和对数运算，使其可通过高效的移位和加法操作在神经形态硬件上实现。
- **ANN-to-SNN转换训练流程**：首先在连续域训练一个使用上述近似激活函数的量化ANN，然后将其确定性地转换为完全脉冲化的SNN模型，确保了模型的性能和部署可行性。

**3. 主要实验结果**
在Metr-la、PEMS-BAY等四个标准多元时间序列预测数据集上的实验表明：
- **预测精度**：SpikySpace达到了与当前最佳SNN模型（如SpikeSTAG）相当或更优的预测精度（例如，在Electricity数据集上R²达到0.992）。
- **能效优势**：与基于Transformer的先进模型（iTransformer和iSpikformer）相比，SpikySpace的**估计能耗分别降低了98.73%和96.24%**，同时模型参数量减少了约45-47%。
- **消融研究**：验证了所提近似激活函数在不同量化比特下性能损失极小（<1%），且模型对内部时间步长设置具有鲁棒性。

**4. 研究意义和价值**
SpikySpace成功地将现代序列建模（状态空间模型）与神经形态计算的能效优势相融合，为在资源受限的边缘设备上部署高效、实时的时序预测系统开辟了一条切实可行的路径。它不仅是首个完全脉冲化的SSM，其提出的硬件友好型设计原则也为未来开发更复杂的神经形态算法提供了重要参考。

## 问答对

### 问题 1

**Q**: 这篇论文的核心创新点是什么，他想解决什么问题，怎么解决的？

**A**: ## 论文核心分析：SpikySpace

### **一、 论文旨在解决的核心问题**
论文瞄准**边缘设备上时间序列预测任务面临的严峻能效挑战**。具体问题包括：
1.  **高能耗与低延迟需求的矛盾**：在交通管理、工业监测等场景，模型需在资源受限的边缘设备上实现近实时预测，但现有高性能模型（如Transformer）计算复杂、能耗高。
2.  **现有脉冲神经网络模型的效率瓶颈**：虽然SNN具有事件驱动、超低功耗的潜力，但当前基于SNN的时间序列预测器（如SpikeTCN, iSpikformer）大多继承了Transformer的复杂结构，其注意力机制仍存在二次复杂度，未能充分发挥SNN的能效优势。
3.  **状态空间模型与SNN的兼容性问题**：SSM（如Mamba）虽能以线性复杂度建模长序列，但其连续状态更新和复杂的激活函数（如Softplus, SiLU）与SNN的离散、稀疏计算模式不兼容，难以直接用于构建全脉冲SSM。

### **二、 论文的核心创新点**
论文提出了 **SpikySpace**，这是**首个完全脉冲化的状态空间模型**，旨在将SSM的长序列建模能力与SNN的超高能效相结合。其创新点可归纳为以下三个层面：

#### **1. 模型架构创新：全脉冲状态空间模型**
- **核心思想**：将SSM的连续状态更新机制彻底改造为**事件驱动的脉冲选择性扫描**。
- **关键技术**：
    - **选择性扫描的脉冲化**：将SSM中的状态更新 `h_t = Ā_t * h_{t-1} + B̄_t * s_t` 中的矩阵乘法替换为基于脉冲事件的累加操作，仅在脉冲发生时进行计算。
    - **状态与输出的脉冲编码**：在SSM的循环扫描过程中，对隐藏状态 `h_t` 和输出 `y_t` 都引入了脉冲神经元，确保整个信息流是稀疏的脉冲序列。
    - **线性复杂度**：继承了SSM的选择性扫描机制，将计算复杂度从Transformer的 `O(L²)` 降低到 `O(L)`，同时通过脉冲实现了计算的动态稀疏性。

#### **2. 硬件友好激活函数创新：PTSoftplus 与 PTSiLU**
- **问题**：标准SSM依赖的Softplus和SiLU函数包含指数、对数运算，在神经形态芯片上实现成本极高。
- **解决方案**：提出了两种基于**2的幂次方**的近似函数。
    - **PTSoftplus**：用分段函数 `(2^x, x<xc; x+C, x≥xc)` 近似Softplus。
    - **PTSiLU**：用分段函数 `(-2^x, x<xc; 2^{-x-1}+x+C̄, x≥xc)` 近似SiLU。
- **优势**：
    - **理论保证**：论文证明了这两个函数是连续可微的，且与原函数的偏差有界（PTSoftplus偏差<0.914，PTSiLU偏差<0.316），保证了训练稳定性。
    - **硬件高效**：2的幂次方运算在硬件上可通过高效的**位移操作**实现，彻底消除了昂贵的指数和除法运算。

#### **3. 训练与部署流程创新**
- **训练策略**：采用 **ANN-to-SNN转换** 路径。首先，使用提出的PTSoftplus/PTSiLU和低比特量化训练一个轻量化的ANN。然后，通过确定性转换将该ANN映射为SNN。
- **能效优化技巧**：通过调整脉冲神经元的阈值和权重缩放（`θ‘ = T * θ`, `W‘ = T * W`），将脉冲发放率从近100%显著降低至26.8%，从而大幅减少 synaptic operations（突触操作），这是SNN能耗的主要来源。

### **三、 解决方案的总结**
SpikySpace通过 **“架构脉冲化” + “组件硬件友好化” +  “训练流程优化”** 的组合拳，系统性地解决了边缘时间序列预测的能效难题：

1.  **用脉冲SSM替代脉冲Transformer**：从根本上规避了注意力机制的二次复杂度，利用SSM的线性扫描和结构化记忆。
2.  **用脉冲事件驱动计算**：将SSM的密集矩阵乘法转化为基于脉冲索引的稀疏累加，使计算量与实际信号变化（脉冲率）成正比，而非固定时间步长。
3.  **用硬件友好组件替换复杂运算**：设计的PTSoftplus/PTSiLU使得整个模型核心运算（激活、门控）可通过加法和位移完成，与神经形态硬件（如Intel Loihi）完美契合。
4.  **通过转换与优化保证性能与效率**：ANN-SNN转换保留了ANN的精度，而阈值/权重缩放技术则压低了运行时的脉冲密度，实现了极致的能效。

### **实际价值与效果**
- **性能**：在多个标准数据集（Metr-la, Electricity等）上，预测精度达到或超过了最先进的SNN基线（如SpikeSTAG）和强大的ANN基线（如iTransformer）。
- **能效**：**实现了革命性的能耗降低**。与iTransformer和iSpikformer相比，估计能耗分别降低了 **98.73%** 和 **96.24%**。模型参数量也减少了约45%。
- **意义**：为在严格功耗和延迟预算下的边缘实时预测（如智能传感器、车载计算、工业物联网）提供了一条切实可行且可扩展的技术路径，真正实现了 **“精度与可部署性的平衡”**。


### 问题 2

**Q**: 请用一段话总结这篇论文，明确说明：论文试图解决的核心问题；提出的主要方法或框架；最终取得的主要效果或结论。要求语言简洁、信息密度高，不要复述摘要原文。

**A**: 这篇论文旨在解决边缘设备上时间序列预测任务中**高能耗与低延迟需求**与现有高性能模型（尤其是基于Transformer的SNN模型）**计算复杂度高、能效低**之间的矛盾。为此，论文提出了 **SpikySpace**，这是首个**完全脉冲化的状态空间模型**。其核心方法是将高效的线性复杂度状态空间模型（SSM，如Mamba）与脉冲神经网络（SNN）的事件驱动特性深度融合：通过**脉冲驱动的选择性扫描**机制，仅在发生脉冲事件时更新状态，将密集的矩阵乘法替换为稀疏的累加操作；同时，为兼容神经形态硬件，设计了**PTSoftplus和PTSiLU**两种基于二次幂的轻量级激活函数近似，以高效的位移操作替代昂贵的指数和除法运算。实验表明，SpikySpace在多个时间序列预测数据集上取得了与先进Transformer和SNN模型**相竞争的预测精度**，同时**大幅降低了能耗**（例如，相比iTransformer和iSpikformer，能耗分别降低了98.73%和96.24%），成功地在预测性能和能源效率之间实现了更优的平衡。


### 问题 3

**Q**: 这篇论文相对于已有工作有哪些明确的创新点？请逐条列出，并对每一条说明：相比以往方法改进或不同之处在哪里，以及该创新解决了什么具体问题或带来了什么优势。

**A**: ## SpikySpace 论文创新点分析

这篇论文提出了一种名为 **SpikySpace** 的脉冲状态空间模型，旨在实现高效能的时间序列预测。其核心创新点在于将**状态空间模型**的计算效率与**脉冲神经网络**的能效优势相结合，并针对神经形态硬件进行了专门优化。以下是其相对于已有工作的明确创新点：

---

### 1. **首次提出完全脉冲化的状态空间模型**
- **改进/不同之处**：
    - 以往将SSM与SNN结合的工作（如SpikingSSMs、P-SpikeSSM等）通常只在部分模块使用脉冲，或保留连续的激活函数和密集乘法运算。
    - SpikySpace 是**第一个完全脉冲化的SSM**，其潜在状态更新和非线性变换均通过离散的脉冲驱动计算实现。模型中的 `SpikingMamba` 模块，包括选择性扫描，都使用脉冲神经元 (`𝒮𝒩`) 进行处理。
- **解决的问题/带来的优势**：
    - **根本上提升了能效**：完全的事件驱动计算避免了每一步的密集矩阵乘法，计算和内存访问仅与脉冲事件（信号变化）相关，而非序列长度。
    - **与神经形态硬件原生兼容**：二进制脉冲和稀疏更新是神经形态芯片（如Loihi）的理想计算模式。
    - **保持了SSM的长程依赖建模能力**：通过线性复杂度的选择性扫描机制，在保持极低能耗的同时，解决了传统SNN模型（如基于Transformer的SNN）难以高效处理长序列的问题。

### 2. **提出针对神经形态硬件的轻量级激活函数近似：PTSoftplus 和 PTSiLU**
- **改进/不同之处**：
    - 标准SSM（如Mamba）依赖 `Softplus` 和 `SiLU` 等激活函数，这些函数涉及昂贵的指数和对数运算，在神经形态硬件上难以高效实现。
    - SpikySpace 提出了 **PTSoftplus** 和 **PTSiLU**。它们是**分段函数**，在 `x < x_c` 时使用 `2^x`（幂次运算），在 `x >= x_c` 时使用线性函数 `x + C`。所有参数均基于 `ln2` 推导，确保函数连续可微。
- **解决的问题/带来的优势**：
    - **硬件友好**：`2^x` 运算可通过高效的**移位操作**实现，完全避免了指数、对数和除法运算。
    - **保持性能**：论文通过理论证明（Lemma 1-4）和实验验证，表明这些近似函数与原函数偏差有界（函数值偏差<0.914，导数偏差<0.371），在量化至低比特（如1-bit）时仍能保持模型精度，解决了复杂激活函数阻碍SSM在SNN中部署的关键障碍。
    - **支持低比特量化训练**：这些近似函数与ANN-to-SNN转换流程中的量化方案（LSQ）协同工作，为生成高效的脉冲模型奠定了基础。

### 3. **设计脉冲驱动的选择性扫描模块**
- **改进/不同之处**：
    - 在标准Mamba中，选择性扫描涉及矩阵指数运算 `exp(ΔA)`。
    - SpikySpace 将其近似为 **`2^{⌊ΔA⌉}`**（幂次运算）。同时，**状态 `h_t` 和输出 `y_t` 在扫描过程中被强制编码为脉冲**（见算法2第16、17行），即 `𝐡_t = 𝒮𝒩(𝐀̄_t 𝐡_{t-1} + 𝐁̄_t 𝐮_t)`。
- **解决的问题/带来的优势**：
    - **将线性复杂度扫描转化为脉冲事件驱动**：不仅计算复杂度从Transformer的 `O(L²)` 降为 `O(L)`，而且每一步的更新从密集的浮点矩阵乘法变为稀疏的脉冲累加。
    - **二次节能**：1) 线性复杂度本身降低了计算量；2) 脉冲表示将乘法转换为加法，并利用时空稀疏性进一步减少操作数。
    - **结构化记忆得以保留**：SSM的隐状态 `h_t` 作为紧凑的记忆载体，通过脉冲进行更新和传递，实现了长期信息的有效维护。

### 4. **采用结合量化与ANN-to-SNN转换的训练方法**
- **改进/不同之处**：
    - 并非直接使用替代梯度法训练SNN，而是采用**两阶段法**：1) 使用提出的 `PTSoftplus/PTSiLU` 和**低比特量化**训练一个ANN；2) 通过**确定性ANN-to-SNN转换**将量化激活映射为脉冲事件。
    - 在转换时，通过**缩放阈值 (`θ’ = T·θ`) 和权重 (`W’ = T·W`)** 的技术，将脉冲发放率从近100%显著降低至26.8%。
- **解决的问题/带来的优势**：
    - **提升训练稳定性和最终精度**：避免了直接训练SNN的梯度近似难题，利用成熟的ANN训练技术获得高性能。
    - **实现极低的脉冲发放率**：低发放率是SNN能效的关键。该技术有效抑制了由于SSM中数值累积导致的高频脉冲问题，使能量消耗与信息内容而非时间步长成正比。
    - **与硬件估算匹配**：极低的发放率直接转化为模拟中惊人的能效提升（相比iTransformer节能98.73%）。

---

### **总结：核心创新价值**
SpikySpace 的系统性创新在于**构建了一个从算法原理、组件设计到训练部署都完全面向神经形态计算的高效时间序列预测模型**。它并非简单地将现有SNN与SSM拼接，而是深度重构了SSM的计算范式，使其融入脉冲计算的稀疏性与事件驱动特性，同时通过硬件友好的近似解决了部署中的关键计算瓶颈。最终，在多个真实数据集上达到了与先进Transformer模型相媲美的精度，同时实现了**数量级级别的能耗降低**，为边缘设备上的实时、低功耗时序分析提供了切实可行的解决方案。


### 问题 4

**Q**: 论文在实验或评估中最终实现了怎样的效果？请说明使用了哪些数据集和评价指标，与哪些基线方法进行了对比，以及在关键指标上的主要性能提升或结论。如果论文未给出明确的定量结果，也请说明原因。

**A**: ## 论文实验与评估效果总结

### 一、 使用的数据集
论文在四个广泛使用的多元时间序列预测基准数据集上进行了评估，涵盖了交通、太阳能和电力消耗等不同领域。具体信息如下：

| 数据集 | 样本数 | 变量数 | 采样间隔 | 序列长度 | 训练/验证/测试集划分比例 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **METR-LA** | 34,272 | 207 | 5分钟 | 12 | (0.7, 0.2, 0.1) |
| **PEMS-BAY** | 52,116 | 325 | 5分钟 | 12 | (0.7, 0.2, 0.1) |
| **Solar-Energy** | 52,560 | 137 | 10分钟 | 168 | (0.6, 0.2, 0.2) |
| **Electricity** | 26,304 | 321 | 1小时 | 168 | (0.6, 0.2, 0.2) |

### 二、 评价指标
论文采用以下两个与数据尺度无关的指标，以便在不同量级的数据集间进行公平比较：
1.  **决定系数 (R²)**：衡量模型对数据方差的解释能力。**值越高越好**，最大为1。
    ```math
    R^{2}=1-\frac{\sum_{t}(y_{t}-\hat{y}_{t})^{2}}{\sum_{t}(y_{t}-\bar{y})^{2}}
    ```
2.  **根相对平方误差 (RRSE)**：衡量预测误差相对于数据均方根的比率。**值越低越好**。
    ```math
    \mathrm{RRSE}=\sqrt{\frac{\sum_{t}(y_{t}-\hat{y}_{t})^{2}}{\sum_{t}(y_{t}-\bar{y})^{2}}}
    ```

### 三、 对比的基线方法
论文将 **SpikySpace** 与以下方法进行了对比，包括传统人工神经网络（ANN）和脉冲神经网络（SNN）：
- **ANN基线**:
    - **GRU**: 经典的循环神经网络模型。
    - **iTransformer**: 最先进的Transformer变体，代表了全精度ANN的强性能。
- **SNN基线**:
    - **SpikeTCN**: 基于卷积的SNN。
    - **SpikeRNN**: 基于循环单元的SNN。
    - **iSpikformer**: 基于Transformer架构的SNN。
    - **SpikeSTAG**: 结合图神经网络的时空SNN。

### 四、 关键性能与结论
论文在四个数据集上，针对3、6、12、24四个预测步长进行了全面评估。主要结论如下：

#### 1. 预测准确性：**达到或超越SOTA SNN，媲美高性能ANN**
- **整体表现**：SpikySpace在多个数据集和预测步长上取得了**最佳或接近最佳**的R²和RRSE值。
- **具体优势**：
    - 在 **Electricity** 数据集上，SpikySpace取得了 **R² = 0.992** 和 **RRSE = 0.162** 的**最佳成绩**，超越了所有SNN和ANN基线（iTransformer R²=0.983）。
    - 在 **METR-LA** 数据集上，平均R²达到 **0.778**，比之前最好的SNN模型（SpikeSTAG）**提升了3.3%**。
    - 在 **Solar** 和 **PEMS-BAY** 数据集上，SpikySpace的表现与最佳基线模型**持平或非常接近**。
- **结论**：SpikySpace成功**弥合了SNN与密集ANN之间的精度差距**，证明了其建模长程依赖关系的能力。

#### 2. 能源效率：**实现数量级级别的能耗降低**
这是论文最核心的贡献之一。在**Electricity**数据集（预测步长=3）上的能量分析显示：

| 模型 | 骨干网络 | 是否为SNN | 参数量 (M) | 能耗 (mJ) | 相对iTransformer能耗降低 | R² |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **SpikySpace** | SSM | **是** | **0.868** | **0.12** | **98.73%** | **0.994** |
| SpikeSTAG | LSTM/Transformer | 是 | 1.566 | 4.39 | 53.64% | 0.987 |
| iSpikformer | Transformer | 是 | 1.634 | 3.19 | 66.30% | 0.982 |
| iTransformer | Transformer | 否 | 1.634 | 9.47 | / | 0.983 |

- **关键数据**：
    - 与最先进的ANN模型 **iTransformer** 相比，SpikySpace能耗降低了 **98.73%**。
    - 与最先进的SNN模型 **iSpikformer** 相比，能耗降低了 **96.24%**。
    - SpikySpace的**参数量也显著减少**（约为基线模型的53%-55%），进一步降低了内存占用。
- **结论**：SpikySpace在保持高精度的同时，实现了**极致的能源效率**，验证了其“全脉冲状态空间模型”设计在硬件友好性和节能方面的巨大优势。

#### 3. 消融实验验证
- **激活函数近似（PTSoftplus/PTSiLU）**：实验证明，即使在**1比特量化**的极端情况下，使用近似函数带来的精度损失也**小于1%**，验证了其有效性和鲁棒性。
- **脉冲时间步长（T）**：实验表明，在`T=1`到`T=15`的范围内，模型性能**保持稳定**。最终选择`T=3`作为精度和效率的平衡点。

### 总结
**SpikySpace** 通过创新的**全脉冲状态空间模型**设计，在四个标准时间序列预测数据集上实现了：
- **准确性**：达到或超越了现有最先进的SNN模型，并与高性能ANN模型（如iTransformer）精度相当。
- **效率**：在保持高精度的前提下，将**能耗降低了1-2个数量级**（相比iTransformer节能98.73%），同时模型参数量减少近一半。

这标志着在**将现代序列建模能力与神经形态计算的高能效特性相结合**的道路上迈出了关键一步，为在严格功耗和延迟约束下的边缘设备部署高效时间序列预测系统提供了切实可行的方案。


## 相关链接

- [arXiv 页面](https://arxiv.org/abs/2601.02411v1)
- [HTML 版本](https://arxiv.org/html/2601.02411v1)
